# Examples 1:
# Project with source files only - no artifacts present
# Output should be empty since only source/config files are in the change list

# Example 2:
# Project with artifacts:
# *.log
# Expected output includes compiled files, dependencies, env files, editor configs, etc.